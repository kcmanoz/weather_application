import { TransformUnitValuePipe } from './transform-unit-value.pipe';

describe('TransformUnitValuePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformUnitValuePipe();
    expect(pipe).toBeTruthy();
  });
});
